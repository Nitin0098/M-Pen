
STM32 Firmware is supposed to be plashed to STM32f401CCU6 it handles sensors reading, sensor callibration and set reset cycles of magnetometers, while live streaming the sensor data to my laptop via USB cdc/Serial Communication.

Python Script is the main program responsible for dipole orientation tracking , callibration methods, and algorithm to convert the rolling of magnetic ball into x and y plot. this python script uses the data recived from serial port to execute its functions. Although these functions could have been integrated to STM32 firmware which was indeed my first plan, but this python script exists because the dipole Orientation tracking method and x and y plot algorithm is not perfect and requires multiple iterations of experiments with multiple techniques and methods, having a seprate python script on laptop itself helps me iterate and experiment much faster because i dont have to flash the stm each time which would require seprate debugging each time and python is easier to understand too.
